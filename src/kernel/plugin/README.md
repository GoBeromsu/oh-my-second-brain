# Plugin seam

This directory is the future plugin integration seam. It re-exports the `SearchBackend` contract and deliberately contains no registry logic, versioned schemas, trust store, lockfile, firewall, user-level registry, or dynamic loading.
