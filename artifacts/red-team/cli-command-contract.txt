=== retired ===

$ oms query
[oms] Unknown command: query

oh-my-second-brain — Oh My Second Brain convention layer for Obsidian vaults

Usage:
  oh-my-second-brain setup [--vault <path>] [--yes] [--suggest-fields] [--install-claude]
  oh-my-second-brain install [--vault <path>] [--runtime <auto|all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain uninstall [--runtime <all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain update [--check] [--dry-run] [--yes] [--runtime <auto|all|claude|codex|hermes>] [--vault <path>]
  oh-my-second-brain doctor [--vault <path>] [--verbose] [--json] [--max <n>]
  oh-my-second-brain audit [--vault <path>] [--folder <name>] [--json] [--suggest-fields]
  oh-my-second-brain lint [--vault <path>] [--verbose] [--json]
  oh-my-second-brain link --vault <path> --folder <name> [--folder <name> ...] [--no-convention-note]
  oh-my-second-brain linkify [--vault <path>] [--folder <name>] [--apply --yes]
  oh-my-second-brain semantic <query|status|get|multi-get|vsearch|search|sync|update|embed|collection|context|cleanup|serve|http> [options]
  oh-my-second-brain mcp [--vault <path>]
  oh-my-second-brain hook pre-tool-use [--vault <path>]
  oh-my-second-brain hook post-tool-use [--vault <path>]

Compatibility alias: oms <command>

Commands:
  setup    Adopt an existing vault into the Oh My Second Brain convention.
  install  Install Oh My Second Brain host adapters and MCP registration.
  uninstall Remove Oh My Second Brain host adapters and MCP registration.
  update   Check for or apply an explicit package update, then refresh host adapters.
  doctor   Validate vault frontmatter against the active ontology, aggregated by field and concept.
  audit    CI-usable ontology lint: doctor checks with non-zero exit on violations.
             --folder <name>   Restrict the scan to one top-level vault folder.
             --json             Emit structured JSON instead of console text.
             --suggest-fields   Report undeclared observed fields and enum-value drift.
  lint     Check vault link health: broken [[wikilinks]] and orphan notes.
  link     Bridge an external repo to scoped vault folders via gitignored symlinks.
  linkify  Propose [[wikilinks]] to term notes across existing notes; report-only by default.
             --folder <name>   Restrict the scan and writes to one top-level vault folder.
             --apply --yes     Rewrite notes in place; --apply alone refuses and writes nothing.
  semantic Native markdown semantic index/search/get commands.
  mcp      Start the read/status MCP stdio server.
  hook     Vault guard hooks for Claude Code PreToolUse / PostToolUse events.
             pre-tool-use  Read PreToolUse JSON from stdin; block unregistered folder creation.
             post-tool-use Read PostToolUse JSON from stdin; audit frontmatter + trigger graph build.

Options:
  --vault <path>   Path to the vault root (default: current directory).
  --yes            Non-interactive setup, uninstall confirmation, or update execution.
  --suggest-fields During setup --yes, add conservative observed frontmatter fields to concepts.
  --install-claude Print Claude Code plugin install and MCP registration commands (dry-run).
  --runtime <name> Select host runtime (default: auto for install, all for uninstall).
  --dry-run        Preview host config changes without writing files.
  --execute        Allow external host CLIs such as `claude` to run when available.
  --verbose        doctor/lint: list every affected note instead of a summary.
  --json           doctor/lint/audit: emit machine-readable output as JSON.
  --max <n>        doctor --verbose: max notes listed per concept (default 50).
  --folder <name>  audit/linkify: restrict scan; link: vault folder/subpath to expose (repeatable for link).
  --apply          linkify: rewrite notes in place; must be combined with --yes.
  --no-convention-note
                  link: skip writing the managed OMS usage block to AGENTS.md.

exit=1

$ oms status
[oms] Unknown command: status

oh-my-second-brain — Oh My Second Brain convention layer for Obsidian vaults

Usage:
  oh-my-second-brain setup [--vault <path>] [--yes] [--suggest-fields] [--install-claude]
  oh-my-second-brain install [--vault <path>] [--runtime <auto|all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain uninstall [--runtime <all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain update [--check] [--dry-run] [--yes] [--runtime <auto|all|claude|codex|hermes>] [--vault <path>]
  oh-my-second-brain doctor [--vault <path>] [--verbose] [--json] [--max <n>]
  oh-my-second-brain audit [--vault <path>] [--folder <name>] [--json] [--suggest-fields]
  oh-my-second-brain lint [--vault <path>] [--verbose] [--json]
  oh-my-second-brain link --vault <path> --folder <name> [--folder <name> ...] [--no-convention-note]
  oh-my-second-brain linkify [--vault <path>] [--folder <name>] [--apply --yes]
  oh-my-second-brain semantic <query|status|get|multi-get|vsearch|search|sync|update|embed|collection|context|cleanup|serve|http> [options]
  oh-my-second-brain mcp [--vault <path>]
  oh-my-second-brain hook pre-tool-use [--vault <path>]
  oh-my-second-brain hook post-tool-use [--vault <path>]

Compatibility alias: oms <command>

Commands:
  setup    Adopt an existing vault into the Oh My Second Brain convention.
  install  Install Oh My Second Brain host adapters and MCP registration.
  uninstall Remove Oh My Second Brain host adapters and MCP registration.
  update   Check for or apply an explicit package update, then refresh host adapters.
  doctor   Validate vault frontmatter against the active ontology, aggregated by field and concept.
  audit    CI-usable ontology lint: doctor checks with non-zero exit on violations.
             --folder <name>   Restrict the scan to one top-level vault folder.
             --json             Emit structured JSON instead of console text.
             --suggest-fields   Report undeclared observed fields and enum-value drift.
  lint     Check vault link health: broken [[wikilinks]] and orphan notes.
  link     Bridge an external repo to scoped vault folders via gitignored symlinks.
  linkify  Propose [[wikilinks]] to term notes across existing notes; report-only by default.
             --folder <name>   Restrict the scan and writes to one top-level vault folder.
             --apply --yes     Rewrite notes in place; --apply alone refuses and writes nothing.
  semantic Native markdown semantic index/search/get commands.
  mcp      Start the read/status MCP stdio server.
  hook     Vault guard hooks for Claude Code PreToolUse / PostToolUse events.
             pre-tool-use  Read PreToolUse JSON from stdin; block unregistered folder creation.
             post-tool-use Read PostToolUse JSON from stdin; audit frontmatter + trigger graph build.

Options:
  --vault <path>   Path to the vault root (default: current directory).
  --yes            Non-interactive setup, uninstall confirmation, or update execution.
  --suggest-fields During setup --yes, add conservative observed frontmatter fields to concepts.
  --install-claude Print Claude Code plugin install and MCP registration commands (dry-run).
  --runtime <name> Select host runtime (default: auto for install, all for uninstall).
  --dry-run        Preview host config changes without writing files.
  --execute        Allow external host CLIs such as `claude` to run when available.
  --verbose        doctor/lint: list every affected note instead of a summary.
  --json           doctor/lint/audit: emit machine-readable output as JSON.
  --max <n>        doctor --verbose: max notes listed per concept (default 50).
  --folder <name>  audit/linkify: restrict scan; link: vault folder/subpath to expose (repeatable for link).
  --apply          linkify: rewrite notes in place; must be combined with --yes.
  --no-convention-note
                  link: skip writing the managed OMS usage block to AGENTS.md.

exit=1

$ oms get
[oms] Unknown command: get

oh-my-second-brain — Oh My Second Brain convention layer for Obsidian vaults

Usage:
  oh-my-second-brain setup [--vault <path>] [--yes] [--suggest-fields] [--install-claude]
  oh-my-second-brain install [--vault <path>] [--runtime <auto|all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain uninstall [--runtime <all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain update [--check] [--dry-run] [--yes] [--runtime <auto|all|claude|codex|hermes>] [--vault <path>]
  oh-my-second-brain doctor [--vault <path>] [--verbose] [--json] [--max <n>]
  oh-my-second-brain audit [--vault <path>] [--folder <name>] [--json] [--suggest-fields]
  oh-my-second-brain lint [--vault <path>] [--verbose] [--json]
  oh-my-second-brain link --vault <path> --folder <name> [--folder <name> ...] [--no-convention-note]
  oh-my-second-brain linkify [--vault <path>] [--folder <name>] [--apply --yes]
  oh-my-second-brain semantic <query|status|get|multi-get|vsearch|search|sync|update|embed|collection|context|cleanup|serve|http> [options]
  oh-my-second-brain mcp [--vault <path>]
  oh-my-second-brain hook pre-tool-use [--vault <path>]
  oh-my-second-brain hook post-tool-use [--vault <path>]

Compatibility alias: oms <command>

Commands:
  setup    Adopt an existing vault into the Oh My Second Brain convention.
  install  Install Oh My Second Brain host adapters and MCP registration.
  uninstall Remove Oh My Second Brain host adapters and MCP registration.
  update   Check for or apply an explicit package update, then refresh host adapters.
  doctor   Validate vault frontmatter against the active ontology, aggregated by field and concept.
  audit    CI-usable ontology lint: doctor checks with non-zero exit on violations.
             --folder <name>   Restrict the scan to one top-level vault folder.
             --json             Emit structured JSON instead of console text.
             --suggest-fields   Report undeclared observed fields and enum-value drift.
  lint     Check vault link health: broken [[wikilinks]] and orphan notes.
  link     Bridge an external repo to scoped vault folders via gitignored symlinks.
  linkify  Propose [[wikilinks]] to term notes across existing notes; report-only by default.
             --folder <name>   Restrict the scan and writes to one top-level vault folder.
             --apply --yes     Rewrite notes in place; --apply alone refuses and writes nothing.
  semantic Native markdown semantic index/search/get commands.
  mcp      Start the read/status MCP stdio server.
  hook     Vault guard hooks for Claude Code PreToolUse / PostToolUse events.
             pre-tool-use  Read PreToolUse JSON from stdin; block unregistered folder creation.
             post-tool-use Read PostToolUse JSON from stdin; audit frontmatter + trigger graph build.

Options:
  --vault <path>   Path to the vault root (default: current directory).
  --yes            Non-interactive setup, uninstall confirmation, or update execution.
  --suggest-fields During setup --yes, add conservative observed frontmatter fields to concepts.
  --install-claude Print Claude Code plugin install and MCP registration commands (dry-run).
  --runtime <name> Select host runtime (default: auto for install, all for uninstall).
  --dry-run        Preview host config changes without writing files.
  --execute        Allow external host CLIs such as `claude` to run when available.
  --verbose        doctor/lint: list every affected note instead of a summary.
  --json           doctor/lint/audit: emit machine-readable output as JSON.
  --max <n>        doctor --verbose: max notes listed per concept (default 50).
  --folder <name>  audit/linkify: restrict scan; link: vault folder/subpath to expose (repeatable for link).
  --apply          linkify: rewrite notes in place; must be combined with --yes.
  --no-convention-note
                  link: skip writing the managed OMS usage block to AGENTS.md.

exit=1

$ oms multi-get
[oms] Unknown command: multi-get

oh-my-second-brain — Oh My Second Brain convention layer for Obsidian vaults

Usage:
  oh-my-second-brain setup [--vault <path>] [--yes] [--suggest-fields] [--install-claude]
  oh-my-second-brain install [--vault <path>] [--runtime <auto|all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain uninstall [--runtime <all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain update [--check] [--dry-run] [--yes] [--runtime <auto|all|claude|codex|hermes>] [--vault <path>]
  oh-my-second-brain doctor [--vault <path>] [--verbose] [--json] [--max <n>]
  oh-my-second-brain audit [--vault <path>] [--folder <name>] [--json] [--suggest-fields]
  oh-my-second-brain lint [--vault <path>] [--verbose] [--json]
  oh-my-second-brain link --vault <path> --folder <name> [--folder <name> ...] [--no-convention-note]
  oh-my-second-brain linkify [--vault <path>] [--folder <name>] [--apply --yes]
  oh-my-second-brain semantic <query|status|get|multi-get|vsearch|search|sync|update|embed|collection|context|cleanup|serve|http> [options]
  oh-my-second-brain mcp [--vault <path>]
  oh-my-second-brain hook pre-tool-use [--vault <path>]
  oh-my-second-brain hook post-tool-use [--vault <path>]

Compatibility alias: oms <command>

Commands:
  setup    Adopt an existing vault into the Oh My Second Brain convention.
  install  Install Oh My Second Brain host adapters and MCP registration.
  uninstall Remove Oh My Second Brain host adapters and MCP registration.
  update   Check for or apply an explicit package update, then refresh host adapters.
  doctor   Validate vault frontmatter against the active ontology, aggregated by field and concept.
  audit    CI-usable ontology lint: doctor checks with non-zero exit on violations.
             --folder <name>   Restrict the scan to one top-level vault folder.
             --json             Emit structured JSON instead of console text.
             --suggest-fields   Report undeclared observed fields and enum-value drift.
  lint     Check vault link health: broken [[wikilinks]] and orphan notes.
  link     Bridge an external repo to scoped vault folders via gitignored symlinks.
  linkify  Propose [[wikilinks]] to term notes across existing notes; report-only by default.
             --folder <name>   Restrict the scan and writes to one top-level vault folder.
             --apply --yes     Rewrite notes in place; --apply alone refuses and writes nothing.
  semantic Native markdown semantic index/search/get commands.
  mcp      Start the read/status MCP stdio server.
  hook     Vault guard hooks for Claude Code PreToolUse / PostToolUse events.
             pre-tool-use  Read PreToolUse JSON from stdin; block unregistered folder creation.
             post-tool-use Read PostToolUse JSON from stdin; audit frontmatter + trigger graph build.

Options:
  --vault <path>   Path to the vault root (default: current directory).
  --yes            Non-interactive setup, uninstall confirmation, or update execution.
  --suggest-fields During setup --yes, add conservative observed frontmatter fields to concepts.
  --install-claude Print Claude Code plugin install and MCP registration commands (dry-run).
  --runtime <name> Select host runtime (default: auto for install, all for uninstall).
  --dry-run        Preview host config changes without writing files.
  --execute        Allow external host CLIs such as `claude` to run when available.
  --verbose        doctor/lint: list every affected note instead of a summary.
  --json           doctor/lint/audit: emit machine-readable output as JSON.
  --max <n>        doctor --verbose: max notes listed per concept (default 50).
  --folder <name>  audit/linkify: restrict scan; link: vault folder/subpath to expose (repeatable for link).
  --apply          linkify: rewrite notes in place; must be combined with --yes.
  --no-convention-note
                  link: skip writing the managed OMS usage block to AGENTS.md.

exit=1

$ oms vsearch
[oms] Unknown command: vsearch

oh-my-second-brain — Oh My Second Brain convention layer for Obsidian vaults

Usage:
  oh-my-second-brain setup [--vault <path>] [--yes] [--suggest-fields] [--install-claude]
  oh-my-second-brain install [--vault <path>] [--runtime <auto|all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain uninstall [--runtime <all|claude|codex|hermes>] [--dry-run] [--execute] [--yes] [--json]
  oh-my-second-brain update [--check] [--dry-run] [--yes] [--runtime <auto|all|claude|codex|hermes>] [--vault <path>]
  oh-my-second-brain doctor [--vault <path>] [--verbose] [--json] [--max <n>]
  oh-my-second-brain audit [--vault <path>] [--folder <name>] [--json] [--suggest-fields]
  oh-my-second-brain lint [--vault <path>] [--verbose] [--json]
  oh-my-second-brain link --vault <path> --folder <name> [--folder <name> ...] [--no-convention-note]
  oh-my-second-brain linkify [--vault <path>] [--folder <name>] [--apply --yes]
  oh-my-second-brain semantic <query|status|get|multi-get|vsearch|search|sync|update|embed|collection|context|cleanup|serve|http> [options]
  oh-my-second-brain mcp [--vault <path>]
  oh-my-second-brain hook pre-tool-use [--vault <path>]
  oh-my-second-brain hook post-tool-use [--vault <path>]

Compatibility alias: oms <command>

Commands:
  setup    Adopt an existing vault into the Oh My Second Brain convention.
  install  Install Oh My Second Brain host adapters and MCP registration.
  uninstall Remove Oh My Second Brain host adapters and MCP registration.
  update   Check for or apply an explicit package update, then refresh host adapters.
  doctor   Validate vault frontmatter against the active ontology, aggregated by field and concept.
  audit    CI-usable ontology lint: doctor checks with non-zero exit on violations.
             --folder <name>   Restrict the scan to one top-level vault folder.
             --json             Emit structured JSON instead of console text.
             --suggest-fields   Report undeclared observed fields and enum-value drift.
  lint     Check vault link health: broken [[wikilinks]] and orphan notes.
  link     Bridge an external repo to scoped vault folders via gitignored symlinks.
  linkify  Propose [[wikilinks]] to term notes across existing notes; report-only by default.
             --folder <name>   Restrict the scan and writes to one top-level vault folder.
             --apply --yes     Rewrite notes in place; --apply alone refuses and writes nothing.
  semantic Native markdown semantic index/search/get commands.
  mcp      Start the read/status MCP stdio server.
  hook     Vault guard hooks for Claude Code PreToolUse / PostToolUse events.
             pre-tool-use  Read PreToolUse JSON from stdin; block unregistered folder creation.
             post-tool-use Read PostToolUse JSON from stdin; audit frontmatter + trigger graph build.

Options:
  --vault <path>   Path to the vault root (default: current directory).
  --yes            Non-interactive setup, uninstall confirmation, or update execution.
  --suggest-fields During setup --yes, add conservative observed frontmatter fields to concepts.
  --install-claude Print Claude Code plugin install and MCP registration commands (dry-run).
  --runtime <name> Select host runtime (default: auto for install, all for uninstall).
  --dry-run        Preview host config changes without writing files.
  --execute        Allow external host CLIs such as `claude` to run when available.
  --verbose        doctor/lint: list every affected note instead of a summary.
  --json           doctor/lint/audit: emit machine-readable output as JSON.
  --max <n>        doctor --verbose: max notes listed per concept (default 50).
  --folder <name>  audit/linkify: restrict scan; link: vault folder/subpath to expose (repeatable for link).
  --apply          linkify: rewrite notes in place; must be combined with --yes.
  --no-convention-note
                  link: skip writing the managed OMS usage block to AGENTS.md.

exit=1

=== canonical ===

$ oms semantic query
{
  "available": false,
  "reason": "graph-only engine: embedding provider unavailable. Configure embeddings via OMS_EMBEDDING_PROVIDER + OMS_EMBEDDING_MODEL (and provider auth env vars, e.g. UPSTAGE_API_KEY) for engine semantic ops.",
  "hits": []
}
exit=1

$ oms semantic status
{
  "available": true,
  "storage": "oms-native-json",
  "models": {
    "embedding": "deferred:graph-only"
  }
}
exit=0

$ oms semantic get
Usage: oms semantic get <target>
exit=1

$ oms semantic multi-get
Usage: oms semantic multi-get <target...>
exit=1

$ oms semantic vsearch
{
  "available": false,
  "reason": "graph-only engine: embedding provider unavailable. Configure embeddings via OMS_EMBEDDING_PROVIDER + OMS_EMBEDDING_MODEL (and provider auth env vars, e.g. UPSTAGE_API_KEY) for engine semantic ops.",
  "hits": []
}
exit=1

$ oms semantic get references/clean-architecture.md
{
  "available": true,
  "documents": [
    {
      "target": "references/clean-architecture.md",
      "path": "references/clean-architecture.md",
      "content": "---\ntitle: \"Clean Architecture: A Craftsman's Guide to Software Structure and Design\"\nsource-url: https://www.oreilly.com/library/view/clean-architecture-a/9780134494272/\nauthor:\n  - Robert C. Martin\ntags:\n  - software-architecture\n  - design\n  - clean-code\n---\n\n## Summary\n\nRobert C. Martin's *Clean Architecture* argues that the primary value of software is its\nability to change — not its current behavior. The book presents the SOLID principles and\nshows how they compose into architectural patterns that keep the cost of change low.\n\n## Key Ideas\n\n- **Dependency Rule**: source code dependencies must point inward toward higher-level policies.\n- **Screaming Architecture**: the architecture should declare the intent of the system, not its\n  framework choices.\n- **Use Cases as first-class citizens**: the application's use cases should be visible at the\n  top level of the source tree.\n\n## Synthesis\n\nThe boundary between \"what changes together\" and \"what changes for different reasons\" is the\nfundamental organizing principle. Components that are deployed together should be designed\ntogether; those with different rates of change or different owners should be separated.\n",
      "docid": "references/clean-architecture.md",
      "title": "\"Clean Architecture: A Craftsman's Guide to Software Structure and Design\""
    }
  ]
}
exit=0

$ oms semantic multi-get references/clean-architecture.md notes/idea.md
{
  "available": true,
  "documents": [
    {
      "target": "references/clean-architecture.md",
      "path": "references/clean-architecture.md",
      "content": "---\ntitle: \"Clean Architecture: A Craftsman's Guide to Software Structure and Design\"\nsource-url: https://www.oreilly.com/library/view/clean-architecture-a/9780134494272/\nauthor:\n  - Robert C. Martin\ntags:\n  - software-architecture\n  - design\n  - clean-code\n---\n\n## Summary\n\nRobert C. Martin's *Clean Architecture* argues that the primary value of software is its\nability to change — not its current behavior. The book presents the SOLID principles and\nshows how they compose into architectural patterns that keep the cost of change low.\n\n## Key Ideas\n\n- **Dependency Rule**: source code dependencies must point inward toward higher-level policies.\n- **Screaming Architecture**: the architecture should declare the intent of the system, not its\n  framework choices.\n- **Use Cases as first-class citizens**: the application's use cases should be visible at the\n  top level of the source tree.\n\n## Synthesis\n\nThe boundary between \"what changes together\" and \"what changes for different reasons\" is the\nfundamental organizing principle. Components that are deployed together should be designed\ntogether; those with different rates of change or different owners should be separated.\n",
      "docid": "references/clean-architecture.md",
      "title": "\"Clean Architecture: A Craftsman's Guide to Software Structure and Design\""
    },
    {
      "target": "notes/idea.md",
      "path": "notes/idea.md",
      "content": "---\ntitle: \"Idea: convention-as-data for knowledge graphs\"\ncreated: 2026-05-31\n---\n\nWhat if the metadata schema for a knowledge base were itself stored as structured data inside\nthe vault, rather than being hardcoded into a tool?\n\nThe user could evolve the schema incrementally — adding a field at a time — and any tool that\nreads the vault would automatically pick up the new convention.\n\nThis feels like the right inversion: the data defines the rules, not the other way around.\n",
      "docid": "notes/idea.md",
      "title": "\"Idea: convention-as-data for knowledge graphs\""
    }
  ]
}
exit=0
