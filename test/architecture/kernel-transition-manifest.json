{
  "schemaVersion": 1,
  "purpose": "Shrink-only inventory of src/ top-level entries that MUST disappear before the kernel migration closes. Seeded before any move so every migration PR can prove a strict shrink against its merge base. Entries may only be removed, never added. PR 7c deletes this file once the list is empty.",
  "arithmetic": "19 must-disappear + 2 persisting (cli, mcp) + 3 added (kernel, vendors, assets) = the final 5 top-level entries. kernel-transitional is a temporary 4th addition deleted in the same PR that empties this manifest.",
  "persisting": ["cli", "mcp"],
  "additionAllowlist": ["kernel", "vendors", "assets", "kernel-transitional"],
  "finalTopLevel": ["assets", "cli", "kernel", "mcp", "vendors"],
  "mustDisappear": [
    "capture",
    "conventions",
    "core",
    "e2e.test.ts",
    "engine",
    "graph",
    "harness",
    "hook",
    "index.test.ts",
    "index.ts",
    "install",
    "link",
    "ontology",
    "plugin.test.ts",
    "retrieve",
    "runtime",
    "setup",
    "setup.e2e.test.ts",
    "update"
  ]
}
