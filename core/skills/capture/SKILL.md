---
name: capture
description: Capture knowledge into the vault. Use the write skill.
---

# Skill: capture

Use the `write` skill. Call MCP `write` with `mode: "create"` (or `append` / `update`).

Do not use host Write/Edit for vault `.md` files. The kernel owns the `.oms` contract.
